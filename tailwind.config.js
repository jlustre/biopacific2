import { defineConfig } from '@tailwindcss/vite';

export default defineConfig({
  theme: {
    extend: {
      colors: {
        primary: '#047857',
        secondary: '#1f2937', 
        accent: '#06b6d4',
      }
    }
  }
});